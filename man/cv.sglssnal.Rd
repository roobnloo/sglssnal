% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cv.sglssnal.R
\name{cv.sglssnal}
\alias{cv.sglssnal}
\title{Cross-validation for sglssnal}
\usage{
cv.sglssnal(
  A,
  b,
  grp_vec,
  grp_idx,
  alpha = 0.75,
  lambda = NULL,
  nlambda = 100,
  lambda_min_ratio = 1e-04,
  nfolds = 5,
  foldid = NULL,
  printyes = TRUE,
  stoptol = 1e-06,
  stoptolcv = 1e-04,
  quietall = FALSE,
  ...
)
}
\arguments{
\item{A}{\eqn{n \times p} design matrix.}

\item{b}{\eqn{n} response vector.}

\item{grp_vec}{Vector of indicies of variables in each group.
If there are \eqn{g} groups and \code{G_i} contains the indices of the
\code{i}-th group, then \code{grp_vec} should be the concatenated vector
\code{c(G_1, G_2, ..., G_g)}.}

\item{grp_idx}{\eqn{2 \times g} matrix indexing the groups in \code{grp_vec}.
\code{grp_vec[grp_idx[1, i]:grp_idx[2, i]]} are the indices of the \code{i}-th
group.}

\item{alpha}{Mixing parameter determining the relative weight of the \eqn{\ell_1} and
group \eqn{\ell_2} penalty. Must be in \eqn{[0, 1]}. Default is \code{0.75}.}

\item{lambda}{Vector of lambda parameters to tune. If \code{NULL}, the
lambda path is automatically generated by \code{sglssnal} using \code{nlambda} and \code{lambda_min_ratio}.
If supplied, \code{nlambda} and \code{lambda_min_ratio} are ignored.}

\item{nlambda}{Number of lambda values to use when lambda is NULL.}

\item{lambda_min_ratio}{Minimum ratio of the smallest to largest lambda when lambda is NULL.}

\item{nfolds}{Number of folds for cross-validation.
Ignored if \code{foldid} is provided.}

\item{foldid}{Vector of integers specifying the fold for each observation.
If \code{NULL}, a default assignment is used.}

\item{printyes}{Print progress in main loop. Default is \code{TRUE}.}

\item{stoptol}{Tolerance for stopping criteria. Default is \code{1e-6}.}

\item{stoptolcv}{Tolerance for convergence in cross-validation folds.
May be set to a smaller value than \code{stoptol} for faster convergence.
Default is \code{1e-4}.}

\item{quietall}{Suppress all messages.}

\item{...}{Additional arguments passed to \code{\link[=sglssnal]{sglssnal()}}.}
}
\value{
List of class \verb{cv.sglssanl, sglssnal}, containing the following components:
\itemize{
\item \code{obj}: Matrix containing primal and dual objective values for the solution.
\item \code{x}: Matrix of primal variables, with each column corresponding to a lambda.
\item \code{x0}: Vector of intercept values for each lambda.
\item \code{y}: Matrix of y dual variables, with each column corresponding to a lambda.
\item \code{z}: Matrix of z dual variables, with each column corresponding to a lambda.
\item \code{info}: List containing information about the optimization process.
\item \code{cv_info}: List containing the lambdas, cross-validation errors, and
the index of the optimal lambda.
}
}
\description{
Perform cross-validation for the sglssnal algorithm over
a path of lambda values.
}
