% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sglssnal.R
\name{sglssnal}
\alias{sglssnal}
\title{Run Sparse-Group Lasso via Semismooth Newton Augmented Lagrangian}
\usage{
sglssnal(
  A,
  b,
  grp_vec,
  grp_idx,
  lambda,
  alpha,
  pfgroup = rep(1, ncol(grp_idx)),
  stoptol = 1e-06,
  stopopt = 2L,
  printyes = TRUE,
  printsub = FALSE,
  maxit = 5000L,
  Lip = NULL,
  y0 = NULL,
  z0 = NULL,
  x0 = NULL
)
}
\arguments{
\item{A}{\eqn{n \times p} design matrix.}

\item{b}{\eqn{n} response vector.}

\item{grp_vec}{Vector of indicies of variables in each group.
If there are \eqn{g} groups and \code{G_i} contains the indices of the
\code{i}-th group, then \code{grp_vec} should be the concatenated vector
\code{c(G_1, G_2, ..., G_g)}.}

\item{grp_idx}{\eqn{2 \times g} matrix indexing the groups in \code{grp_vec}.
\code{grp_vec[grp_idx[1, i]:grp_idx[2, i]]} are the indices of the \code{i}-th
group.}

\item{lambda}{Penalty parameter together with \code{alpha}.
\code{alpha * lambda} is the \eqn{\ell_1} penalty and \code{(1 - alpha) * lambda}
is the group \eqn{\ell_2} penalty.}

\item{alpha}{Determines the relative weight of the \eqn{\ell_1} and
group \eqn{\ell_2} penalty. Must be in \eqn{[0, 1]}.}

\item{pfgroup}{Penalty factor for each group in the group lasso.
Default is a vector of ones, indicating no weighting for any group.}

\item{stoptol}{Tolerance for stopping criteria. Default is \code{1e-6}.}

\item{stopopt}{Stopping criteria. 1: relative duality gap and feasibility,
2: KKT conditions, 3: dual feasibility and relative duality gap,
4: dual feasibility and absolute duality gap. Default is \code{2L}.}

\item{printyes}{Print progress in main loop. Default is \code{TRUE}.}

\item{printsub}{Print progress in subproblem. Default is \code{FALSE}.}

\item{maxit}{Maximum number of iterations. Default is \code{5000L}.}

\item{Lip}{Lipschitz constant for the step size.
Automatically computed as the maximum eigenvalue of AA' if \code{NULL}.}

\item{y0}{optional initialization vector for dual variable \code{y}}

\item{z0}{optional initialization vector for dual variable \code{z}}

\item{x0}{optional initialization vector for primal variable \code{x}}
}
\value{
List of class \code{sglssnal} containing the following components:
\itemize{
\item \code{obj}: Vector containing primal and dual objective values at the solution.
\item \code{x}: The primal variable of interest.
\item \code{y}: The y dual variable.
\item \code{z}: The z dual variable.
\item \code{info}: List containing information about the optimization process such as
relative gap, iteration number, and run time.
}
}
\description{
Fits a sparse-group lasso model using
second-order information to solve the dual problem.
The penalty function is given by
\deqn{\Phi(x) = \lambda_1 ||x||_1 + \lambda_2 \sum_{i=1}^g w_i ||x_{G_i}||_2}
where \eqn{G_i} is the \eqn{i}-th group and \eqn{w_i} its penalty factor.
The primal problem is given by
\deqn{\min_{x \in \mathbb{R}^p}\; \frac{1}{2} ||Ax - b||_2^2 + \Phi(x)}
while the dual problem has the form
\deqn{\max_{y \in \mathbb{R}^n, z \in \mathbb{R}^p}\; -\langle b, y\rangle - \frac{1}{2}||y||_2^2 - \Phi^\ast(z) \text{s.t.} A^\top y + z = 0}
where \eqn{\Phi^\ast(z)} denotes the Fenchel conjugate of \eqn{\Phi}.
The algorithm is based on the work of Zhang et al. (2020).
}
\references{
Zhang, Y., Zhang, N., Sun, D., & Toh, K. C. (2020).
\emph{An efficient Hessian based algorithm for solving large-scale
sparse group Lasso problems.} Mathematical Programming, 179, 223-263.
\doi{https://doi.org/10.1007/s10107-018-1329-6}.
}
